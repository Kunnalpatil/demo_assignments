# Web framework for creating the chat interface
streamlit==1.38.0

# LangChain components for conversation management and LLM integration
langchain==0.3.0
langchain-groq==0.2.0
langchain-community==0.3.0

# For Text-to-Speech (TTS) functionality
gTTS==2.5.3

# For generating unique session IDs
uuid==1.30 

# For logging and debugging
logging==0.5.1.2  

# For temporary file handling in TTS
tempfile==3.12  # tempfile is part of Python's standard library, but specifying for clarity

# For encoding audio data in base64 for playback
base64==3.12  # base64 is part of Python's standard library, but specifying for clarity